<div class="max-w-6xl mx-auto min-h-screen">
  <div class="bg-white border-4 border-black shadow-2xl">
    <div class="bg-gray-100 px-8 py-6 border-b-4 border-black flex justify-between items-center">
      <h1 class="text-3xl font-mono tracking-wide">仕訳一覧</h1>
      <%= link_to "新規仕訳", new_journal_entry_path, class: "bg-black hover:bg-gray-800 focus:bg-gray-700 focus:ring-2 focus:ring-gray-400 focus:outline-none text-white py-2 px-4 border-2 border-black font-mono tracking-widest" %>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full border-collapse">
        <thead class="bg-gray-100">
          <tr>
            <th class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm tracking-wider">仕訳日</th>
            <th class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm tracking-wider">摘要</th>
            <th class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm tracking-wider">借方科目</th>
            <th class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm tracking-wider">貸方科目</th>
            <th class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm tracking-wider">金額</th>
            <th class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm tracking-wider">操作</th>
          </tr>
        </thead>
        <tbody class="bg-white">
          <% if @journal_entries.any? %>
            <% @journal_entries.each do |entry| %>
              <tr class="hover:bg-yellow-100">
                <td class="px-4 py-3 border-[0.5] border-black whitespace-nowrap font-mono text-sm">
                  <%= entry.entry_date.strftime("%Y年%m月%d日") %>
                </td>
                <td class="px-4 py-3 border-[0.5] border-black whitespace-nowrap font-mono text-sm">
                  <%= entry.summary.present? ? entry.summary : "—" %>
                </td>
                <td class="px-4 py-3 border-[0.5] border-black font-mono text-sm">
                  <% debit_lines = entry.journal_entry_lines.select { |line| line.side == "debit" } %>
                  <% debit_lines.each do |line| %>
                    <div><%= line.account.name %></div>
                  <% end %>
                </td>
                <td class="px-4 py-3 border-[0.5] border-black font-mono text-sm">
                  <% credit_lines = entry.journal_entry_lines.select { |line| line.side == "credit" } %>
                  <% credit_lines.each do |line| %>
                    <div><%= line.account.name %></div>
                  <% end %>
                </td>
                <td class="px-4 py-3 border-[0.5] border-black whitespace-nowrap font-mono text-sm text-right">
                  <% amount = entry.journal_entry_lines.where(side: "debit").sum(:amount) %>
                  <%= number_to_currency(amount, unit: "¥", precision: 0, delimiter: ",") %>
                </td>
                <td class="px-4 py-3 border-[0.5] border-black text-center font-mono text-sm">
                  <div class="flex justify-center space-x-2">
                    <%= link_to "編集", edit_journal_entry_path(entry), class: "text-black hover:bg-gray-200 px-2 py-1 border border-black font-mono" %>
                    <%= button_to "削除", journal_entry_path(entry), method: :delete,
                                  data: { turbo_confirm: "本当に削除しますか？" },
                                  class: "text-black hover:bg-gray-200 px-2 py-1 border border-black font-mono bg-transparent" %>
                  </div>
                </td>
              </tr>
            <% end %>
          <% else %>
            <tr>
              <td colspan="6" class="px-4 py-8 border-[0.5] border-black text-center font-mono text-black">
                仕訳が登録されていません。
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="flex justify-center py-2">
     <%= paginate @journal_entries %>
    </div>
  </div>
</div>
